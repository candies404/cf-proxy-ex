## 主页代码
```html
const mainPage = `

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://dash.cloudflare.com/favicon.ico" type="image/x-icon">
    <title>CF Reverse Proxy 使用说明</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333;
        }
        p {
            color: #555;
        }
        code {
            background: #eee;
            padding: 2px 4px;
            border-radius: 4px;
            cursor: pointer;
            position: relative;
        }
        code:hover::after {
            content: '点击复制';
            position: absolute;
            top: -25px;
            left: 50%;
            transform: translateX(-50%);
            background: #333;
            color: #fff;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            white-space: nowrap;
        }
        a {
            color: #1a73e8;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        .note {
            background: #fff3cd;
            border: 1px solid #ffeeba;
            color: #856404;
            padding: 10px;
            margin-top: 20px;
            border-radius: 5px;
        }
        .success-message {
            display: none;
            color: #fff;
            background-color: #28a745;
            padding: 10px;
            border-radius: 5px;
            margin-top: 10px;
            text-align: center;
            font-weight: bold;
            animation: fadeInOut 2s forwards;
        }
        @keyframes fadeInOut {
            0%, 100% { opacity: 0; }
            10%, 90% { opacity: 1; }
        }
        .input-group {
            display: flex;
            margin-top: 20px;
            max-width: 600px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 4px;
            overflow: hidden;
        }
        .input-group input {
            flex-grow: 1;
            padding: 10px 12px;
            border: none;
            outline: none;
            font-size: 16px;
        }
        .input-group button {
            padding: 10px 20px;
            background-color: #f0f0f0;
            color: #333;
            border: none;
            border-left: 1px solid #ccc;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s, color 0.3s;
        }
        .input-group button:hover {
            background-color: #1a73e8;
            color: white;
        }
        /* 添加移动端响应式样式 */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
                margin: 10px;
            }
            
            h1 {
                font-size: 24px;
            }
            
            h2 {
                font-size: 20px;
            }
            
            .input-group {
                flex-direction: column;
                border: none;
            }
            
            .input-group input {
                border: 1px solid #ccc;
                border-radius: 4px;
                margin-bottom: 10px;
            }
            
            .input-group button {
                border: none;
                border-radius: 4px;
                width: 100%;
                background-color: #1a73e8;
                color: white;
            }
            
            code {
                word-break: break-all;
                display: inline-block;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Cloudflare Reverse Proxy 使用说明</h1>
        <p>本项目地址：<a href="https://github.com/1234567Yang/cf-proxy-ex" target="_blank">https://github.com/1234567Yang/cf-proxy-ex</a></p>
        <h2>搭建教程</h2>
        <p>请参考本项目的快速开始指南：<a href="https://github.com/1234567Yang/cf-proxy-ex?tab=readme-ov-file#%E5%BF%AB%E9%80%9F%E5%BC%80%E5%A7%8B" target="_blank">快速开始</a></p>
        <h2>使用方法</h2>
        <p>在任意网址前面加上 <code id="copy-text"></code> 即可使用。例如：</p>
        <p>原网址：<a href="https://github.com" target="_blank">https://github.com</a></p>
        <p>代理后的网址：<span id="proxy-example"></span></p>
        <div class="input-group">
            <input type="text" id="url-input" placeholder="输入要代理的网址">
            <button onclick="goToProxy()">跳转</button>
        </div>
        <div class="success-message" id="success-message">复制成功！</div>
        <h2>注意事项</h2>
        <div class="note">
            <p>请不要通过在线代理登录任何网站，以保护您的隐私和安全。</p>
        </div>
    </div>
    <script>
        function getCurrentDomain() {
            return window.location.origin + '/';
        }
    
        function updateExamples() {
            const currentDomain = getCurrentDomain();
            document.getElementById('copy-text').textContent = currentDomain;
            document.getElementById('proxy-example').innerHTML = '<a href="' + currentDomain + 'https://github.com" target="_blank">' + currentDomain + 'https://github.com</a>';
        }
    
        updateExamples();
    
        document.getElementById('copy-text').addEventListener('click', function() {
            const text = this.textContent;
            navigator.clipboard.writeText(text).then(function() {
                const message = document.getElementById('success-message');
                message.style.display = 'block';
                setTimeout(() => {
                    message.style.display = 'none';
                }, 2000);
            }, function(err) {
                console.error('复制失败: ', err);
            });
        });
    
        function goToProxy() {
            const input = document.getElementById('url-input');
            const url = input.value.trim();
            if (url) {
                const currentDomain = getCurrentDomain();
                const proxyUrl = currentDomain + (url.startsWith('http') ? '' : 'https://') + url;
                window.open(proxyUrl, '_blank');
            } else {
                alert('请输入有效的网址');
            }
        }
    
        document.getElementById('url-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                goToProxy();
            }
        });
    </script>
</body>
</html>
`;
```


## 提示使用代理代码
```html
const proxyHintInjection = `
//---***========================================***---提示使用代理---***========================================***---

setTimeout(() => {
  if (document.readyState === 'complete' || document.readyState === 'interactive') {
    var hint = \`Warning: You are currently using a web proxy, the original link is \${window.location.pathname}. Please note that you are using a proxy, and do not log in to any website. Click to close this hint. 警告：您当前正在使用网络代理，原始链接为\${window.location.pathname}。请注意您正在使用代理，请勿登录任何网站。单击关闭此提示。\`;
    console.log(1);
    document.body.insertAdjacentHTML(
      'afterbegin', 
      \`<div style="position:fixed;left:0;top:0;width:100%;padding:10px;z-index:9999;user-select:none;cursor:pointer;background:#cce5ff;border:1px solid #004085;color:#004085;text-align:center;border-radius:5px;" id="__PROXY_HINT_DIV__" onclick="document.getElementById('__PROXY_HINT_DIV__').remove();">
        \${hint}
      </div>\`    
    );
  } else {
    alert(\`Warning: You are currently using a web proxy, the original link is \${window.location.pathname}. Please note that you are using a proxy, and do not log in to any website.\`);
  }
}, 3000);
`;
```
## 密码页面
```html
const pwdPage = `
<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="theme-color" content="#000000">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <title>密码设置</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🔒</text></svg>">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            background-image: url('[https://api.gumengya.com/Api/FjImg?format=image](https://api.dwo.cc/api/bing)');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            position: relative;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }
        
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1;
        }

        .container {
            position: relative;
            z-index: 2;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .password-form {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            padding: 35px;
            border-radius: 16px;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.36);
            border: 1px solid rgba(255, 255, 255, 0.18);
            width: min(90%, 400px);
            animation: fadeIn 0.5s ease-out;
        }

        input {
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 15px;
            border-radius: 8px;
            width: 100%;
            font-size: 16px;
            height: 50px;
            margin-bottom: 15px;
            transition: all 0.3s ease;
            letter-spacing: 1px;
        }

        input::placeholder {
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
            font-size: 17px;
            letter-spacing: 1px;
        }

        input:focus::placeholder {
            color: rgba(255, 255, 255, 0.7);
            transition: color 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: rgba(255, 255, 255, 0.5);
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        button {
            background: rgba(255, 255, 255, 0.25);
            color: white;
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 15px;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
            font-weight: 500;
            transition: all 0.3s ease;
            -webkit-tap-highlight-color: transparent;
        }

        button:hover {
            background: rgba(255, 255, 255, 0.35);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        button:active {
            transform: translateY(0);
        }

        .toast {
            position: fixed;
            bottom: 50px;
            left: 50%;
            transform: translateX(-50%);
            background: #3498db;
            color: white;
            padding: 16px 32px;
            border-radius: 12px;
            z-index: 1000;
            font-size: 17px;
            font-weight: 500;
            box-shadow: 0 4px 20px rgba(52, 152, 219, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            letter-spacing: 0.5px;
            animation: toastIn 0.2s ease-out;
        }
        /* 入场动画 */
        @keyframes toastIn {
            from { 
                opacity: 0;
                transform: translate(-50%, 20px);
            }
            to { 
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }
        /* 出场动画 */
        @keyframes toastOut {
            from { 
                opacity: 1;
                transform: translate(-50%, 0);
            }
            to { 
                opacity: 0;
                transform: translate(-50%, -20px);
            }
        }

        @keyframes fadeIn {
            from { 
                opacity: 0;
                transform: translateY(20px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* 加载动画 */
        .loading {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.8);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .loading::after {
            content: '';
            width: 40px;
            height: 40px;
            border: 4px solid #ffffff;
            border-top-color: transparent;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        .loading.active {
            display: flex;
        }

        /* 移动端优化 */
        @media (max-width: 480px) {
            body {
                background-attachment: scroll;
            }

            .password-form {
                margin: 10px;
                padding: 25px;
            }

            @supports (padding: max(0px)) {
                .container {
                    padding-left: max(20px, env(safe-area-inset-left));
                    padding-right: max(20px, env(safe-area-inset-right));
                    padding-top: max(20px, env(safe-area-inset-top));
                    padding-bottom: max(20px, env(safe-area-inset-bottom));
                }
            }
        }

        /* 深色模式 */
        @media (prefers-color-scheme: dark) {
            .password-form {
                background: rgba(0, 0, 0, 0.2);
            }
        }

        /* 减少动画 */
        @media (prefers-reduced-motion: reduce) {
            .password-form, button {
                animation: none;
                transition: none;
            }
        }
    </style>
    <script>
        function setPassword() {
            try {
                const passwordInput = document.getElementById('password');
                const password = passwordInput.value.trim();
                const loading = document.querySelector('.loading');
                
                if (!password) {
                    showToast('请输入密码！');
                    return;
                }

                loading.classList.add('active');

                const cookieDomain = window.location.hostname;
                const oneWeekLater = new Date();
                oneWeekLater.setTime(oneWeekLater.getTime() + (7 * 24 * 60 * 60 * 1000));
                
                // 先清除旧cookie
                document.cookie = "${passwordCookieName}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=" + cookieDomain;
                // 设置新cookie
                document.cookie = "${passwordCookieName}=" + password + "; expires=" + oneWeekLater.toUTCString() + "; path=/; domain=" + cookieDomain;
                
                setTimeout(() => {
                    loading.classList.remove('active');
                    location.reload();
                }, 500);
            } catch(e) {
                console.error('设置密码时出错：', e);
                showToast('设置密码失败：' + e.message);
            }
        }

        function showToast(message) {
            const toast = document.createElement('div');
            toast.className = 'toast';
            toast.textContent = message;
            
            // 移除之前存在的 toast
            const existingToast = document.querySelector('.toast');
            if (existingToast) {
                existingToast.remove();
            }
            
            document.body.appendChild(toast);
            
            // 3秒后开始淡出动画
            setTimeout(() => {
                toast.style.animation = 'toastOut 0.3s ease-out forwards';
                // 等待动画完成后再移除元素
                toast.addEventListener('animationend', () => {
                    toast.remove();
                });
            }, 3000);
        }

        // 添加回车键提交功能
        document.addEventListener('DOMContentLoaded', () => {
            const passwordInput = document.getElementById('password');
            passwordInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    setPassword();
                }
            });

            // 自动聚焦到输入框
            passwordInput.focus();
        });
    </script>
</head>
<body>
    <div class="container">
        <div class="password-form">
            <input id="password" 
                   type="password" 
                   placeholder="请输入密码" 
                   autocomplete="new-password"
                   autocapitalize="off"
                   spellcheck="false">
            <button onclick="setPassword()">确认提交</button>
        </div>
    </div>
    <div class="loading"></div>
</body>
</html>
`;
```
